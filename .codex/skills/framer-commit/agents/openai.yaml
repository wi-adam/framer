interface:
  display_name: "Framer Commit"
  short_description: "Validate and commit Framer changes safely."
  default_prompt: "Use $framer-commit to inspect the Framer worktree, run the locked formatting, clippy, test, docs, and GPU gates that apply, then create a scoped commit only when checks pass."
